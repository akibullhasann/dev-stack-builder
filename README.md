### My Project Name is Dev Stack Builder.

***-a brief description about my project.
--- this website has been made in such a way so that developer can browse the entire website and can discover about the technologies and later they can use these technologies for their own purpose or to improve their career.



### Technoloy that I used for this project.
--- TypeScript with React, Tailwind Css,Vite,JSON, CSS3, React-Toastify, React-Icon and Obviously use little bit of AI to generate Gradient Color.



#### Features of this project
1. Explore technologies--- know the name, rating, difficulty
2. Add your Own stack --- you can choose your preferable technologies in your stack.
3. And obviously it is designed to work smoothly in all devices --- responsive.


#### Now Question Of the answer--

i. What is JSX, and why is it used in React?
-- In simple word JSX is writing HMTL inside JS. And cause of using in react is react actually Component based I mean UI interactive.

ii. What is the difference between props and state?
-- Props is property that is passed to child Component from Parent Component for passing data or value while state works based on rendering. I mean state change that means browser screen need to change.

iii. What does the useState hook do, and where did you use it in this project?
-- useState contains any vaild value for the purpose of changing browser screen. To make website look dynamic easily change the screen and update the screen that fits from data.

iv. What does the useEffect hook do, and why did you need it to load the JSON data?
-- useEffect in normal word is work after rendering the screen that are side task. In other words, first show the screen to the UI and then do the work that left like fetching data. you can think like side task after rendering.

v. Why does every item in a .map() list need a unique key prop?
-- key is needed cause it helps to recognise the list for working later in any logic.

vi. What is conditional rendering? Show one place you used it (example: the empty stack message).
-- conditional rendering,  there would have so many condition, so based on condition i will decide which one i need to the screen. Ahhmm, I think use in Stack section compnenet.

vii .How do you pass data from a parent component to a child component, and how does a child send something back to the parent?
--- I pass data from a parent to a child using PROPS. Child could not send data to the parent.